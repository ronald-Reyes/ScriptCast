About
    Similar to descript, scriptcast is an app that lets user create simple videos, do transcription and TTS, 
    record and save audio, and lastly download the generated video

To run the code locally,

Requirements
1. NodeJS 
2. FFMPEG

Run the ff. commands from ScriptCast Folder
1. npm run build
2. npm run start / npm run build
3. open at localhost:5000



