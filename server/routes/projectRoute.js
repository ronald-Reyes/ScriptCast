const { getAllProjects, getProject } = require("../controllers/userController");

const router = require("express").Router();

//router.post("/getAllProjects", getAllProjects);
//router.post("/getProject", getProject);

module.exports = router;
